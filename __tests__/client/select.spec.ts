describe('Client', () => {
  describe('select', () => {
    it.todo('passes tablename and what to select and query')
    it.todo('selects * from table name if no option is given')

    describe('Concatenating with other clauses', () => {
      it.todo('concatenates with WHERE')
      it.todo('concatenates with COUNT')
      it.todo('concatenates with GroupBy')
    })
  })
})
